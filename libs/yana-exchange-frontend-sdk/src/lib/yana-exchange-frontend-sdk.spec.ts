import { yanaExchangeFrontendSdk } from './yana-exchange-frontend-sdk';

describe('yanaExchangeFrontendSdk', () => {
  it('should work', () => {
    expect(yanaExchangeFrontendSdk()).toEqual('yana-exchange-frontend-sdk');
  });
});
